import { FC } from 'react';
import DefaultLayout from '../../layouts/default';

const Login: FC = () => {
  return (
    <DefaultLayout>
      <h1>Login</h1>
    </DefaultLayout>
  );
};

export default Login;
